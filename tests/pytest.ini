[pytest]
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

python_files=*_tests.py

markers =
    regession: marks tests to be executed as regression tests
    smoke: marks tests to be executed as smoke tests